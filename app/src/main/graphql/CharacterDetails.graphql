query CharacterDetailsQuery($characterId: ID!) {
    character(id: $characterId) {
        id
        name
        status
        species
        type
        gender
        created
        image
    }
}